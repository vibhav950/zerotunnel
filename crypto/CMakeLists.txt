if(WITH_OPENSSL)
  file(GLOB _CRYPTO_BACKEND_SOURCES "*_ossl.c")
elseif(WITH_GNUTLS)
  file(GLOB _CRYPTO_BACKEND_SOURCES "*_gtls.c")
endif()

set(CRYPTO_BACKEND_SOURCES ${_CRYPTO_BACKEND_SOURCES} PARENT_SCOPE)
